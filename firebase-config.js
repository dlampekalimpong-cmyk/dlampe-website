// ============ Firebase configuration ============
// Fill this in with YOUR Firebase project's config (see SETUP-FIREBASE.md).
// Until you do, the site will keep working using the built-in starter
// catalogue in starter-data.js — you just won't be able to add/edit
// products from the admin panel yet.
//
// Where to get these values:
// Firebase Console → Project settings (gear icon) → General tab →
// "Your apps" → Web app → SDK setup and configuration → Config

window.FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
